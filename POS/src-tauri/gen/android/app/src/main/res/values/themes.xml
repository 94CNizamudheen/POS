<resources xmlns:tools="http://schemas.android.com/tools">
    <style name="Theme.pos" parent="Theme.Material3.DayNight.NoActionBar">

        <!-- ── Edge-to-edge: transparent system bars ──────────────────── -->
        <!-- Transparent bars allow the WebView to extend behind them,     -->
        <!-- enabling env(safe-area-inset-*) to report real pixel values.  -->
        <item name="android:statusBarColor">@android:color/transparent</item>
        <item name="android:navigationBarColor">@android:color/transparent</item>

        <!-- Light icons on status/nav bars (app content is mostly dark) -->
        <item name="android:windowLightStatusBar">false</item>
        <item name="android:windowLightNavigationBar" tools:targetApi="o_mr1">false</item>

        <!-- Draw behind system bars & extend into display cutout -->
        <item name="android:windowDrawsSystemBarBackgrounds">true</item>
        <item name="android:fitsSystemWindows">false</item>

        <!-- Extend content into notch/cutout on all edges (API 28+) -->
        <!-- "always" = draw under the cutout in all orientations       -->
        <item name="android:windowLayoutInDisplayCutoutMode" tools:targetApi="p">shortEdges</item>

        <!-- No action bar -->
        <item name="android:windowNoTitle">true</item>
        <item name="android:windowActionBar">false</item>

        <!-- Brand colors -->
        <item name="colorPrimary">@color/primary_blue_light</item>
        <item name="colorPrimaryDark">@color/primary_blue</item>
    </style>
</resources>
